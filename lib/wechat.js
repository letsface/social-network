"use strict";

var Promise = require('bluebird'),
    sdk     = Promise.promisifyAll(require('wechat'));

function Wechat(config){
  /**
   *  create an instance with the supplied config
   */
   
};

module.exports = Wechat;